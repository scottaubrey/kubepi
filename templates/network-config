version: 2
ethernets:
  eth0:
    addresses:
      - '${address}${NODE_NETWORK_CIDR_SUFFIX}'
    optional: false
    routes:
      - to: default
        via: '${NODE_NETWORK_GATEWAY}'
    nameservers:
      search: [${NODE_DOMAIN}]
      addresses:
        - '${NODE_DNSSERVER}'
        - '${NODE_NETWORK_DNSSERVER}'
